# --- Required for 1Password CLI (service account) ---
OP_SERVICE_ACCOUNT_TOKEN=op://…/…/…   # or leave blank and set in systemd/CI
# OP_SERVICE_ACCOUNT_TOKEN_FILE=/path/to/token   # alternative

# --- Output & recording ---
WY_OUT_DIR=output
WY_VIDEO_DIR=                      # set to 'videos' to enable; leave empty to disable
WY_TRACE_DIR=traces
WY_TRACING=0                       # 1 to enable Playwright tracing

# --- Data & inputs ---
WY_DATA_DIR=data
WY_FILINGS_CSV=filings.csv

# --- 1Password vault & items ---
WY_VAULT=Wyoming Filing Bot
WY_ITEM_DEFAULTS=Wyoming Filing Defaults
WY_ITEM_CARD=Credit Card
WY_ITEM_2CAPTCHA=2Captcha API Key
# WY_ITEM_CARD_ID=                # only if multiple items share the same title

# --- Optional overrides/tuning ---
WY_RESULTS_KEEP=3
WY_RUNS_KEEP=3
WY_RUN_ID=
PWDEBUG=
WY_PAUSE=
WY_MAX_CAPTCHA_ATTEMPTS=5
WY_DEFAULTS_JSON=                 # JSON blob to override defaults locally
WY_2CAPTCHA_KEY=                  # only if you want to bypass 1Password and set directly
WY_SKIP_2CAPTCHA_BALANCE_CHECK=0  # set 1 to skip online check if corp proxy blocks TLS